import 'package:flutter/material.dart';

class MyColors {
  // ------------------------------------
  // Original Colors
  // ------------------------------------
  static const Color useYellow = Color(0xfffffc00);
  static const Color useBlack = Colors.black;
  static const Color useWhite = Color(0xFFFFFFFF);

  static const Color hintColor = Color(0xFFA0A0A0); // Gray for hints
  static const Color yellow = Color(0xFFFFF000); // Bright yellow
  static const Color lightYellow = Color(0xFFFFCC00); // For the underline/tab
  static const Color textFieldFill = Color(0xFFF0F0F0);


  // New WalkerScreen Dark Theme Colors


  /// The deepest dark grey background for the overall Scaffold.
  static const Color darkBackground = Color(0xFF121212);

  /// The slightly lighter dark grey for elevated surfaces (Cards, Nav Bar).
  static const Color darkSurface = Color(0xFF1E1E1E);

  /// The primary blue accent color for buttons and selection indicators.
  static const Color accentBlue = Color(0xFF1976D2);

  /// A darker shade of grey, used for elements like the profile image placeholder.
  static const Color darkPlaceholder = Color(0xFF424242);
}